#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int T;
	cin>>T;
	if(T>=1 && T<=100)
	{
	    for(int i=0;i<T;i++)
	    {
	        int X,Y;
	        cin>>X>>Y;
	        if(X>=1 && X<=100 && Y>=1 && Y<=100)
	        {
	            if(X%2==0)
	            {
	                if(Y%2==0)
	                {
	                    cout<<"JANMANSH"<<endl;
	                }
	                else
	                {
	                    cout<<"JAY"<<endl;
	                }
	            }
	            else
	            {
	                if(Y%2==0)
	                {
	                    cout<<"JAY"<<endl;
	                }
	                else
	                {
	                    cout<<"JANMANSH"<<endl;
	                }
	            }
	        }
	    }
	}
	return 0;
}
