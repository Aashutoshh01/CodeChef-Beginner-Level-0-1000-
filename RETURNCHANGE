#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int T;
	cin>>T;
	if(T>=1 && T<=100)
	{
	    for(int i=0;i<T;i++)
	    {
	        int X;
	        cin>>X;
	        if(X>=1 && X<=100)
	        {
	            if(X%10<5)
	            {
	                cout<<100-10*(X/10)<<endl;
	            }
	            else
	            {
	                cout<<100-(10*(X/10)+10)<<endl;
	            }
	        }
	    }
	}
	return 0;
}
