#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int T,N,i;
	cin>>T;
	if(T>=1 && T<=100)
	{
	    for(int i=0;i<T;i++)
	    {
	        cin>>N;
	        if(N>=1 && N<=30)
	        {
	            if(N<=10)
	            {
	                cout<<"LOWER DOUBLE"<<endl;
	            }
	            else if(N>=11 && N<=15)
	            {
	                cout<<"LOWER SINGLE"<<endl;
	            }
	            else if(N>=16 && N<=25)
	            {
	                cout<<"UPPER DOUBLE"<<endl;
	            }
	            else
	            {
	                cout<<"UPPER SINGLE"<<endl;
	            }
	        }
	    }
	}
	return 0;
}
