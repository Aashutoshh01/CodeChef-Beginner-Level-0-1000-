#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int T,X,Y,in;
	cin>>T;
	if(T>=1 && T<=100)
	{
	    for(int i=0;i<T;i++)
	    {
	        cin>>X>>Y;
	        if(X>=1 && X<=100 && Y>=1 && Y<=100)
	        {
	            if(X>Y)
	            {
	                cout<<"SECOND"<<endl;
	            }
	            else if(Y>X)
	            {
	                cout<<"FIRST"<<endl;
	            }
	            else
	            {
	                cout<<"ANY"<<endl;
	            }
	        }
	    }
	}
	return 0;
}
