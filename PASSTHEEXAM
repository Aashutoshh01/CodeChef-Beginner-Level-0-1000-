#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int T,A,B,C,i;
	cin>>T;
	if(T>=1 && T<=1000)
	{
	    for(int i=0;i<T;i++)
	    {
	        cin>>A>>B>>C;
	        if(A>=0 && A<=100 && B>=0 && B<=100 && C>=0 && C<=100)
	        {
	            if(A>=10 && B>=10 && C>=10 && A+B+C>=100)
	            {
	                cout<<"PASS"<<endl;
	            }
	            else
	            {
	                cout<<"FAIL"<<endl;
	            }
	        }
	    }
	}
	return 0;
}
