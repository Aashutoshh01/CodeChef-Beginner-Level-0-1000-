#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int T,A,B,C,D,i;
	cin>>T;
	if(T>=1 && T<=1000)
	{
	    for(int i=0;i<T;i++)
	    {
	        cin>>A>>B;
	        cout<<endl;
	        cin>>C>>D;
	        if(A>=0 && A<=10 && B>=0 && B<=10 && C>=0 && C<=10 && D>=0 && D<=10)
	        {
	            if(C>=A && D>=B)
	            {
	                cout<<"POSSIBLE";
	            }
	            else
	            {
	                cout<<"IMPOSSIBLE";
	            }
	        }
	    }
	}
	return 0;
}
