#include <iostream>
using namespace std;

int main() {
    int T,N,i,temp;
    cin>>T;
    if(T>=1 && T<=1000)
    {
        for(int i=0;i<T;i++)
        {
            cin>>N;
            if(N>=1 && N<=1000000)
            {
                while(N%10==0)
                {
                    N=N/10;
                }
                while(N>0)
                {
                    cout<<N%10;
                    N=N/10;
                }
                cout<<endl;
            }
        }
    }
	return 0;
}

