#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int T;
	cin>>T;
	if(T>=1 && T<=1000)
	{
	    for(int i=0;i<T;i++)
	    {
	        float A,X,B,Y;
	        cin>>A>>X>>B>>Y;
	        if(A>=1 && A<=1000 && X>=1 && X<=1000 && B>=1 && B<=1000 && Y>=1 && Y<=1000)
	        {
	            if(A/X>B/Y)
	            {
	                cout<<"ALICE"<<endl;
	            }
	            else if(B/Y>A/X)
	            {
	                cout<<"BOB"<<endl;
	            }
	            else
	            {
	                cout<<"EQUAL"<<endl;
	            }
	        }
	    }
	}
	return 0;
}
