#include <iostream>
using namespace std;

int main() {
	// your code goes here
	int T,R1,R2,R3,R4,i;
	cin>>T;
	if(T>=1 && T<=20)
	{
	    for(int i=0;i<T;i++)
	    {
	        cin>>R1>>R2>>R3>>R4;
	        if(R1==0 || R1==1 && R2==0 || R2==1 && R3==0 || R3==1 && R4==0 || R4==1)
	        {
	            if(R1==0 && R2==0 && R3==0 && R4==0)
	            {
	                cout<<"IN"<<endl;
	            }
	            else
	            {
	                cout<<"OUT"<<endl;
	            }
	        }
	    }
	}
	return 0;
}
